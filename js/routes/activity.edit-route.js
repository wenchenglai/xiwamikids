App.ActivityEditRoute = Ember.Route.extend({
    beforeModel: function(transition) {
        debugger;
    },
    model: function () {
        debugger;
        //return Em.Object.create({});
        //return this.store.find('person', 1);
    }
});